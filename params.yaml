train:
  n_neighbors: 30
  algorithm: auto
  metric: cosine
  p: 2
  leaf_size: 50
  n_jobs: -1

eval:
  top_k: 10
  test_size: 0.2
  min_interactions: 20
  relevance_threshold: 4.0
  max_users: 200
  random_seed: 42
